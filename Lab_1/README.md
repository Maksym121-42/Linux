# Hello Ubuntu 

Перший навчальний проєкт для перевірки роботи середовища розробки в Ubuntu.

Мета: перевірити готовність системи до написання та запуску програм на мові C.

## 1. Перевірка встановлення компілятора

У терміналі виконуємо:

gcc --version
Приклад результату:

```bash
pirat@AdminUbuntu:~$ gcc --version
gcc (Ubuntu 13.3.0-6ubuntu22.04.1) 13.3.0
```
Це означає, що компілятор GCC встановлений та готовий до використання.

Якщо замість цього з’являється повідомлення про відсутність команди, необхідно встановити пакет build-essential:

- sudo apt install build-essential

Після встановлення слід повторно перевірити версію компілятора.

Переходимо до робочої папки та перевіряємо список файлів у директорії:
``` bash
pirat@AdminUbuntu:~$ cd ~/Desktop/Lab
pirat@AdminUbuntu:~/Desktop/Lab$ ls
```
**hello  hello.c  pr1  pr1.c**

Для запуску виконуваного файлу використовуємо команду:  
./hello  
**Результат:**
Hello Ubuntu

Програма успішно компілюється та запускається в середовищі Ubuntu.
Система повністю готова до подальшої роботи з мовою програмування C


# Завдання 1.11
Опис завдання:
Розробити консольну програму мовою C, яка реалізує просту базу даних студентів.

Програма повинна забезпечувати:
1) додавання записів
2) перегляд записів
3) редагування записів
4) видалення записів
5) пошук за частковим збігом

Кожен запис містить:
1) ім'я студента
2) дату народження
3) адресу


### Структура даних
Для зберігання інформації використовується структура:

struct Student{  
char name[50];  
char birth[20];  
char address[100];  
}; 

### Запуск програми

Після успішної компіляції та запуску програми користувача зустрічає меню   
<img width="387" height="201" alt="image" src="https://github.com/user-attachments/assets/41c50d4b-5149-4355-946d-d3984f30dba0" />
